# 2026-05-31 工作日志

## Speed Dial 项目 - 缓存策略调整
- **农历缓存**：`LUNAR_CACHE_DURATION` 从 24小时 → 1小时（`src/utils/weather.ts` 第102行）
- **天气缓存**：`CACHE_DURATION` 从 30分钟 → 15分钟（`src/utils/weather.ts` 第53行）
- 编译通过，无新增错误

## Speed Dial 项目 - 天气/农历卡片展开同步
- 将 `showForecast`（天气）和 `showDetail`（农历）的内部状态提升到 `App.svelte` 的 `cardExpanded`
- 通过 `expanded` prop + `ontoggle` 回调传给两个卡片组件
- 点击任一卡片→两个卡片同步展开/收起，天气显示后续几天预报，农历显示宜忌等信息
- 涉及文件：`App.svelte`, `WeatherWidget.svelte`, `LunarWidget.svelte`

## Speed Dial 项目 - 搜索改为新窗口打开
- `SearchBox.svelte` 第17行：`window.location.href` → `window.open(url, '_blank')`
- 点击搜索按钮或按 Enter 后在新标签页打开搜索结果

## 已推送至 Gitee
- Commit: `f54a722..39a88e1`, 6 files, +15/-12
- Commit: `39a88e1..56d17db`, 版本号 v1.0.0 底部显示（硬编码 `const VERSION`，不可修改）
- Commit: `56d17db..0a98a2e`, 添加 `BUSINESS_PLAN.md` 盈利方案文档
- Commit: `0a98a2e..25f7402`, 统一命名为 Quick Dial（package.json、README、manifest.json、BUSINESS_PLAN）
- Commit: `25f7402..fdd7f02`, 内置图标选择器：新增 IconPicker 组件，7分类70+图标，移除 favicon 自动获取
