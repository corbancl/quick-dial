# 呲啦起始页 (Quick Dial) 后期更新开发方向规划

> 版本基线：v1.0.12（已四端发布：web / 浏览器扩展 / Emlog 插件 / 飞牛 NAS fpk）
> 规划日期：2026-07-29
> 状态：规划稿（未执行）

## 核心设计底线（所有方向必须遵守）

- **极简一屏**：简单、精美、一屏看完、不需要太多操作。
- 任何新增内容卡 / 组件必须「默认关、可一键隐藏」，禁止堆砌破坏极简定位。
- 四端（web / 扩展 / Emlog 插件 / 飞牛）必须随时保持同源可打包状态。

---

## 一、产品功能演进

| 方向 | 具体项 | 优先级 | 说明 |
|---|---|---|---|
| 智能排序 | 导航卡片按点击频次自动置顶 / 「最近常用」分组 | P1 | 本地统计，不上云可保隐私合规；设置里可开 |
| 搜索增强 | 关键词直达（`gh xxx`→GitHub、`yt xxx`→YouTube）、搜索历史下拉 | P1 | 已有多引擎快切，直达是低成本高感知增量 |
| 内容卡扩展 | 在「天气/农历/6时钟」基础上增：便签、待办、RSS 摘要卡 | P2 | ⚠️ 必须做成可关闭模块，坚守一屏原则 |
| 同步增强 | 现有云同步 + JSON 导入导出，补 WebDAV 自托管同步 + 端到端加密 | P2 | 满足隐私用户；与 Pro 云同步并列 |
| AI 入口 | 搜索栏集成可选 AI 对话（可配置 API） | P3 | 谨慎评估，默认关闭，不破坏极简 |

---

## 二、工程化与四端同步（优先补缺）

| 方向 | 具体项 | 优先级 | 说明 |
|---|---|---|---|
| 版本号单一源 | 以 `public/version.json` 为唯一真相，脚本自动同步 `package.json`、各 `manifest*`、插件端 `quickdial.php`、飞牛 `manifest` | P0 | 已两次踩坑（飞牛 1.0.9 滞后），须根治 |
| 一键出四端 | 升级现有 `fnos_pack.py` / FTP 脚本为 `pnpm release` 流水线：build→插件端路径修复→飞牛 SSH 打包→crx→官网发布 | P0 | 固化人工 6 步，降发版事故率 |
| 出包自动校验 | 每次出包自动 sha256 比对线上/本地 | P1 | 闭环验证，符合「证据→修复→证据」铁律 |
| 自动化测试 | Token 系统 / 主题引擎单测 + 关键视图视觉回归 | P2 | 当前零测试，重构风险高 |
| Monorepo 化 | 四端共享 `src/`，构建配置分离，告别手动复制副本 | P2 | 根治插件端/飞牛端副本漂移根因 |

---

## 三、商业化与分发渠道

| 方向 | 具体项 | 优先级 | 说明 |
|---|---|---|---|
| 商店上架 | Chrome Web Store / Edge Add-ons / Firefox AMO 正式上架 | P1 | 获自然流量，降安装门槛（现仅自建 crx 下载） |
| 飞牛应用中心 | fpk 已就绪，提交飞牛官方应用市场 | P1 | 复用已打通真机打包流程 |
| Emlog 应用中心 | 按既有规范（功能扩展类、纯 Markdown 说明）持续维护 | P1 | 已验证流程，定期跟版 |
| Pro 能力扩容 | 设计工坊/壁纸/时钟/天气/同步基础上，加团队协作导航、自定义子域名部署 | P2 | 提升付费转化点 |
| 订阅后端完善 | `account.php`/`pay.php` 补全续费、优惠码、激活状态回查 | P2 | 当前仅基础，需闭环 |

---

## 四、官网与文档

- 官网截图/演示更新到 v1.0.12 真实界面（P1）
- 新增独立 Changelog 页 + 帮助/文档中心（P2，现仅 about/contact/copyright）
- SEO 基础优化（P2）

---

## 五、技术债与维护

- `website/` 与 `api/` 不进 Git 的 FTP 部署，固化为独立自动化脚本（P1）
- 后端 `api_key` 等密钥确认已 gitignore、未落盘明文（P1）
- Svelte 5 runes 迁移收尾 + 依赖定期升级（P2）

---

## 六、建议路线图（里程碑）

### v1.0.13 — 发版工程化（不发新功能，专治发版风险）
- P0 三项全做：版本号单源、一键四端、出包校验。

### v1.1.0 — 智能 + 上架
- 智能排序 + 搜索直达 + 三商店/飞牛市场上架 + 官网截图更新。

### v1.2.0 — 同步与 Pro
- WebDAV 同步 + 端到端加密 + Pro 扩容 + 帮助中心。

---

## 七、Git / 部署边界（重申硬约束）

- 进 Git：开源前端免费版（`src/`、`public/`、本 ROADMAP.md 等）。
- 不进 Git：`api/`（后端含密钥）、`website/`（商业官网）。两者仅经 FTP 部署生产。
- 原因：开源策略「仅开源前端免费版，后端及商业文档不公开」。
